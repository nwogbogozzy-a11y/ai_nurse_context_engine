---
phase: 04-real-time-system-and-ui-overhaul
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/layout.tsx
  - src/components/NurseSwitcher.tsx
  - src/components/FlagBadge.tsx
  - src/components/NurseActionBar.tsx
  - src/components/PatientCard.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "NurseSwitcher uses shadcn Select + AlertDialog with Radix keyboard navigation"
    - "FlagBadge uses shadcn Badge with semantic color variants for all 5 states"
    - "NurseActionBar uses shadcn Button variants (default, outline, destructive)"
    - "PatientCard uses shadcn Card with last nurse name, admission duration, and active flag count"
    - "Layout header is upgraded with professional styling and shadcn Select NurseSwitcher"
    - "All components use cn() instead of template literals"
  artifacts:
    - path: "src/components/NurseSwitcher.tsx"
      provides: "shadcn Select + AlertDialog nurse identity switcher"
      contains: "SelectTrigger"
    - path: "src/components/FlagBadge.tsx"
      provides: "shadcn Badge with 5 flag state variants"
      contains: "Badge"
    - path: "src/components/NurseActionBar.tsx"
      provides: "shadcn Button nurse action bar"
      contains: "Button"
    - path: "src/components/PatientCard.tsx"
      provides: "Enhanced shadcn Card patient card"
      contains: "CardHeader"
    - path: "src/app/layout.tsx"
      provides: "Upgraded header with shadcn styling"
      contains: "Nurse Context Engine"
  key_links:
    - from: "src/components/NurseSwitcher.tsx"
      to: "@/components/ui/select"
      via: "import"
      pattern: "import.*Select.*from.*ui/select"
    - from: "src/components/PatientCard.tsx"
      to: "@/components/ui/card"
      via: "import"
      pattern: "import.*Card.*from.*ui/card"
---

<objective>
Migrate the header, NurseSwitcher, FlagBadge, NurseActionBar, and PatientCard to shadcn/ui primitives with enhanced visual design.

Purpose: These are the "shell" and "leaf" components seen on every page. Migrating them first means the dashboard looks professional immediately. PatientCard gets the D-18 enhancements (last nurse, admission duration, flag count).
Output: Five migrated components using shadcn primitives, cn() utility, and clinical design tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-CONTEXT.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-01-SUMMARY.md

<interfaces>
From src/lib/types.ts:
```typescript
export type ReviewStatus = 'pending' | 'approved' | 'escalated' | 'overridden' | 'under_review' | 'resolved'
export interface Patient {
  id: string; full_name: string; date_of_birth: string; admission_date: string;
  ward: string; unit_type: string; current_status: string; created_at: string;
}
export interface Note {
  id: string; patient_id: string; flagged: boolean; flag_reason: string;
  nurse_name: string; created_at: string; review_status: ReviewStatus;
  reviewed_by: string | null; reviewed_at: string | null;
  /* ... other fields ... */
}
```

From src/contexts/NurseContext.tsx (nurse identity pattern):
```typescript
export function useNurse(): { nurse: { name: string; shift: string }; setNurse: (n) => void; isDictating: boolean; setIsDictating: (b) => void }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NurseSwitcher to shadcn Select + AlertDialog, and upgrade layout header</name>
  <files>src/components/NurseSwitcher.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/components/NurseSwitcher.tsx
    - src/app/layout.tsx
    - src/contexts/NurseContext.tsx
    - src/components/ui/select.tsx
    - src/components/ui/alert-dialog.tsx
    - .planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
  </read_first>
  <action>
    **NurseSwitcher.tsx rewrite** per D-10:

    Replace the native `<select>` element with shadcn Select (Radix). Replace `window.confirm()` with shadcn AlertDialog.

    Imports needed:
    ```typescript
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
    import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from '@/components/ui/alert-dialog'
    import { cn } from '@/lib/utils'
    ```

    Component structure:
    1. Keep existing `useNurse()` hook usage and nurse options (Sarah Chen - Night, Marcus Webb - Morning)
    2. Replace `<select>` with `<Select value={nurse.name} onValueChange={handleNurseChange}>` containing a `<SelectTrigger>` (min-width 200px, styled with clinical tokens) and `<SelectContent>` with `<SelectItem>` for each nurse
    3. When user selects a different nurse, instead of `window.confirm()`, set a `pendingNurse` state and open the AlertDialog
    4. AlertDialog content per UI-SPEC copywriting contract:
       - Title: "Switch nurse identity"
       - Description: "Switching nurse will attribute all subsequent actions to the new identity. Continue?"
       - Cancel button (outline variant)
       - Continue button (default variant)
    5. On confirm: update nurse via setNurse(), close dialog. On cancel: reset pendingNurse, close dialog.
    6. Keep `isDictating` guard — if dictating, show toast warning instead of opening dialog
    7. Display current nurse name and shift badge next to the Select trigger

    **layout.tsx upgrade** per D-20:

    Upgrade the header bar to professional clinical styling:
    1. Keep NurseProvider wrapping, Toaster, metadata
    2. Header: full-width, sticky top (`sticky top-0 z-50`), height controlled by padding (`py-3`), background `bg-background` with bottom border `border-b border-border`
    3. Left side: accent-colored icon (keep existing SVG) + "Nurse Context Engine" at `text-xl font-semibold text-primary` (Display typography from UI-SPEC: 24px/600 mapped to text-xl which is close enough, or use text-2xl)
    4. Right side: NurseSwitcher component
    5. Max-width container: `max-w-[1600px] mx-auto px-12` (match patient detail page width, use 2xl-ish padding)
    6. Use cn() for all class concatenation
    7. Keep `<html lang="en">` and Inter font link
    8. IMPORTANT: layout.tsx is a server component — do NOT add 'use client'. NurseSwitcher is already a client component.

    Remove the old `bg-surface` background from header — per D-20 it should be `bg-background` (white) with a subtle bottom border.
  </action>
  <verify>
    <automated>cd /Users/gozzynwogbo/ai_domain/n8n/nursingworkflow/nurse_context_engine/src && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NurseSwitcher.tsx contains "import" from "@/components/ui/select"
    - src/components/NurseSwitcher.tsx contains "SelectTrigger"
    - src/components/NurseSwitcher.tsx contains "AlertDialog"
    - src/components/NurseSwitcher.tsx contains "Switch nurse identity"
    - src/components/NurseSwitcher.tsx contains "import { cn } from"
    - src/components/NurseSwitcher.tsx does NOT contain "window.confirm"
    - src/components/NurseSwitcher.tsx does NOT contain "<select"
    - src/app/layout.tsx contains "Nurse Context Engine"
    - src/app/layout.tsx contains "sticky top-0"
    - src/app/layout.tsx contains "max-w-[1600px]"
    - npm run build succeeds
  </acceptance_criteria>
  <done>NurseSwitcher uses shadcn Select + AlertDialog with Radix keyboard nav, layout header is professional with sticky positioning and consistent max-width</done>
</task>

<task type="auto">
  <name>Task 2: Migrate FlagBadge, NurseActionBar, and PatientCard to shadcn</name>
  <files>src/components/FlagBadge.tsx, src/components/NurseActionBar.tsx, src/components/PatientCard.tsx</files>
  <read_first>
    - src/components/FlagBadge.tsx
    - src/components/NurseActionBar.tsx
    - src/components/PatientCard.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/lib/types.ts
    - .planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
  </read_first>
  <action>
    **FlagBadge.tsx rewrite:**

    Replace custom badge with shadcn Badge. Import Badge from `@/components/ui/badge` and cn from `@/lib/utils`.

    The Badge component was patched in Plan 01 with variants: warning, critical, safe, review.

    Map existing 5 states to Badge variants:
    - `type === 'safe'` OR `reviewStatus === 'approved' || 'resolved' || 'overridden'` -> `variant="safe"` with text "Safe" / "Approved" / "Resolved" / "Overridden"
    - `type === 'warning'` AND no resolution -> `variant="warning"` with text "Flagged"
    - `type === 'critical'` AND no resolution -> `variant="critical"` with text "Critical"
    - `reviewStatus === 'under_review'` -> `variant="review"` with text "Under Review"
    - `reviewStatus === 'escalated'` -> `variant="warning"` with text "Escalated"

    Keep existing props interface but ensure it uses the Badge component internally. Add `aria-label` on the Badge for screen readers (e.g., `aria-label="Flag status: Flagged"`).

    Use cn() for all class logic. Named export: `export function FlagBadge(...)`.

    **NurseActionBar.tsx rewrite:**

    Replace custom buttons with shadcn Button. Import Button from `@/components/ui/button` and cn from `@/lib/utils`.

    Per UI-SPEC interaction contracts:
    - Approve: `<Button variant="default" size="sm">` (accent bg from patched Button)
    - Escalate: `<Button variant="outline" size="sm">`
    - Override: `<Button variant="destructive" size="sm">`

    Keep ALL existing functionality:
    - `useNurse()` hook for nurse.name attribution
    - Supabase direct writes for review_status updates
    - Audit log insertion via `insertAuditEntry()`
    - Toast notifications on success/error
    - Disabled state when action already taken
    - Loading spinner during async operations

    Replace template literal class concatenation with cn(). Remove any hardcoded hex values.

    **PatientCard.tsx rewrite** per D-18, D-17:

    Replace custom card with shadcn Card (Card, CardHeader, CardContent, CardFooter). Import from `@/components/ui/card`, Badge from `@/components/ui/badge`, cn from `@/lib/utils`.

    Enhanced card content per D-18:
    1. **Left border color indicator stays** — 4px left border using `border-l-4` with color based on flag status (border-l-flag-safe for safe, border-l-flag-warning for flagged, border-l-flag-critical for critical)
    2. **CardHeader**: Patient name (text-lg font-semibold) + FlagBadge in same row (flex justify-between items-center)
    3. **CardContent**: Ward + Unit type on one line, current status below
    4. **NEW — Three additions per D-18:**
       a. Last documenting nurse name: show `latestNote?.nurse_name` with a small user icon, text-sm text-secondary. Display "No notes yet" if null.
       b. Admission duration: calculate from `patient.admission_date` to today. Display as "Day N" (e.g., "Day 3"). Use: `Math.ceil((Date.now() - new Date(patient.admission_date).getTime()) / 86400000)`
       c. Active unresolved flag count: NEW PROP `unresolvedFlagCount: number` passed from dashboard page. Display as Badge variant="warning" if count > 0, e.g., "2 unresolved flags"
    5. **CardFooter**: Last note timestamp in text-xs text-muted
    6. Click handler: entire card is wrapped in Next.js Link to `/patient/{id}`, or use `onClick` with `router.push`

    Props interface update:
    ```typescript
    interface PatientCardProps {
      patient: Patient
      latestNote: Note | null
      unresolvedFlagCount?: number  // NEW
    }
    ```

    Visual upgrade per D-17:
    - Use Card component for proper rounded corners, shadow-sm on hover
    - Padding: p-6 (24px per spacing scale lg)
    - Gap between elements: space-y-3
    - Hover state: `hover:shadow-md transition-shadow duration-150`
    - Cursor pointer since entire card is clickable

    Use cn() everywhere. Named export: `export function PatientCard(...)`.
  </action>
  <verify>
    <automated>cd /Users/gozzynwogbo/ai_domain/n8n/nursingworkflow/nurse_context_engine/src && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/FlagBadge.tsx contains "import" from "@/components/ui/badge"
    - src/components/FlagBadge.tsx contains "Badge"
    - src/components/FlagBadge.tsx contains "cn("
    - src/components/FlagBadge.tsx does NOT contain backtick template class concatenation for conditional styling (no `${` inside className)
    - src/components/NurseActionBar.tsx contains "import" from "@/components/ui/button"
    - src/components/NurseActionBar.tsx contains 'variant="default"' or "variant='default'" or variant="outline" or variant="destructive"
    - src/components/NurseActionBar.tsx contains "cn("
    - src/components/PatientCard.tsx contains "import" from "@/components/ui/card"
    - src/components/PatientCard.tsx contains "CardHeader" or "Card,"
    - src/components/PatientCard.tsx contains "Day " (admission duration display)
    - src/components/PatientCard.tsx contains "unresolvedFlagCount"
    - src/components/PatientCard.tsx contains "nurse_name" (last documenting nurse)
    - src/components/PatientCard.tsx contains "cn("
    - npm run build succeeds
  </acceptance_criteria>
  <done>FlagBadge uses shadcn Badge with 5 semantic variants, NurseActionBar uses shadcn Button with proper variant mapping, PatientCard uses shadcn Card with three D-18 enhancements (last nurse, admission duration, flag count)</done>
</task>

</tasks>

<verification>
- `cd src && npm run build` passes
- `cd src && npm run lint` passes
- All 5 components import from `@/components/ui/` and use `cn()`
- No `window.confirm` remains in NurseSwitcher
- No native `<select>` remains in NurseSwitcher
- PatientCard displays admission duration and last nurse name
</verification>

<success_criteria>
Header is professional with sticky positioning. NurseSwitcher has Radix keyboard navigation. FlagBadge displays all 5 states with semantic colors. NurseActionBar uses proper button variants. PatientCard shows enhanced information per D-18. All components use cn() and shadcn primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-system-and-ui-overhaul/04-03-SUMMARY.md`
</output>
