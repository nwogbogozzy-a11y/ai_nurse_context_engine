---
phase: 04-real-time-system-and-ui-overhaul
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/ActivityTimeline.tsx
  - src/components/PatientContextSummary.tsx
  - src/components/ProcedureSearch.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "ActivityTimeline uses shadcn Card for timeline entries"
    - "PatientContextSummary uses shadcn Card with skeleton during loading"
    - "ProcedureSearch uses shadcn Card + Button"
    - "All three components use cn() for class merging"
  artifacts:
    - path: "src/components/ActivityTimeline.tsx"
      provides: "shadcn Card audit timeline"
      contains: "cn("
    - path: "src/components/PatientContextSummary.tsx"
      provides: "shadcn Card patient context summary"
      contains: "Card"
    - path: "src/components/ProcedureSearch.tsx"
      provides: "shadcn Card + Button procedure search"
      contains: "Button"
  key_links:
    - from: "src/components/ActivityTimeline.tsx"
      to: "@/components/ui/card"
      via: "import"
      pattern: "import.*Card.*from.*ui/card"
---

<objective>
Migrate the remaining three components (ActivityTimeline, PatientContextSummary, ProcedureSearch) to shadcn/ui primitives.

Purpose: Completes the component-level migration. These are lower-traffic components but still need shadcn treatment for visual consistency across the application.
Output: Three migrated components with shadcn Card, Button, Skeleton, and cn() utility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-CONTEXT.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-01-SUMMARY.md

<interfaces>
From src/lib/types.ts:
```typescript
export interface AuditLogEntry {
  id: string; patient_id: string; nurse_name: string; action_type: string;
  metadata: Record<string, unknown>; created_at: string;
}
export interface PatientSummary {
  id: string; patient_id: string; summary: string; note_count: number;
  generated_at: string; created_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ActivityTimeline, PatientContextSummary, and ProcedureSearch to shadcn</name>
  <files>src/components/ActivityTimeline.tsx, src/components/PatientContextSummary.tsx, src/components/ProcedureSearch.tsx</files>
  <read_first>
    - src/components/ActivityTimeline.tsx
    - src/components/PatientContextSummary.tsx
    - src/components/ProcedureSearch.tsx
    - src/components/ui/card.tsx
    - src/components/ui/button.tsx
    - src/components/ui/skeleton.tsx
    - .planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
  </read_first>
  <action>
    **ActivityTimeline.tsx rewrite:**

    Import: Card, CardContent from `@/components/ui/card`, Badge from `@/components/ui/badge`, cn from `@/lib/utils`.

    Structure:
    1. Keep existing props interface: `{ entries: AuditLogEntry[] }`
    2. Empty state: Card with centered text per UI-SPEC: heading "No activity recorded", body "Actions taken on this patient will appear here with timestamps and nurse attribution."
    3. Timeline entries: each entry in a subtle container (not full Card — use a `div` with `border-l-2 border-border pl-4 py-2` for timeline line effect)
    4. Per entry: nurse name in font-medium, action type as Badge (use default variant for most, warning variant for escalation actions), timestamp in text-xs text-muted, metadata summary if relevant
    5. Action type display mapping (keep existing logic but use Badge component):
       - 'approve-note' -> Badge default, text "Approved"
       - 'escalate-note' -> Badge warning, text "Escalated"
       - 'override-note' -> Badge critical, text "Overridden"
       - 'create-note' -> no badge, just text
       - 'generate-handoff' -> Badge default, text "Generated Handoff"
       - 'confirm-supply' -> Badge default, text "Confirmed Supply"
    6. Use cn() for all class logic. Keep chronological order (newest first per existing fetch).

    **PatientContextSummary.tsx rewrite:**

    Import: Card, CardHeader, CardContent from `@/components/ui/card`, Skeleton from `@/components/ui/skeleton`, cn from `@/lib/utils`.

    Structure:
    1. Keep existing props: `{ summary: PatientSummary | null; loading: boolean }`
    2. Loading state: Card with CardHeader "Patient Context" + CardContent showing 3 Skeleton lines (w-full, w-[85%], w-[70%])
    3. Empty state (no summary, not loading): Card with text "No context summary available yet. Submit notes to generate a patient context summary."
    4. Populated state: Card with CardHeader "Patient Context" + note count + generated timestamp, CardContent with summary text in text-sm leading-relaxed
    5. Use cn() everywhere. Named export.

    **ProcedureSearch.tsx rewrite:**

    Import: Card, CardHeader, CardContent from `@/components/ui/card`, Button from `@/components/ui/button`, Textarea from `@/components/ui/textarea` (if it has a text input) or keep input styling, cn from `@/lib/utils`.

    Structure:
    1. Wrap in Card
    2. CardHeader: "Look Up Supplies" or existing title
    3. CardContent: search input + Button variant="default" for submit
    4. Keep existing functionality: webhook call to supply lookup endpoint, loading state, error handling
    5. During loading: show SupplySkeleton from `@/components/skeletons/SupplySkeleton`
    6. Use cn() everywhere. Named export.
  </action>
  <verify>
    <automated>cd /Users/gozzynwogbo/ai_domain/n8n/nursingworkflow/nurse_context_engine/src && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ActivityTimeline.tsx contains "cn("
    - src/components/ActivityTimeline.tsx contains import from "@/components/ui/badge" or "@/components/ui/card"
    - src/components/ActivityTimeline.tsx contains "No activity recorded"
    - src/components/PatientContextSummary.tsx contains import from "@/components/ui/card"
    - src/components/PatientContextSummary.tsx contains "Card" and "CardHeader"
    - src/components/PatientContextSummary.tsx contains "Skeleton" (loading state)
    - src/components/PatientContextSummary.tsx contains "cn("
    - src/components/ProcedureSearch.tsx contains import from "@/components/ui/card"
    - src/components/ProcedureSearch.tsx contains import from "@/components/ui/button"
    - src/components/ProcedureSearch.tsx contains "cn("
    - npm run build succeeds
  </acceptance_criteria>
  <done>All three remaining components migrated to shadcn primitives with cn(), proper empty states per UI-SPEC copywriting, and Skeleton loading states</done>
</task>

</tasks>

<verification>
- `cd src && npm run build` passes
- All 11 original components now use shadcn primitives and cn()
- No component has template literal class concatenation for conditional styling
</verification>

<success_criteria>
All 11 application components are migrated to shadcn/ui. Every component uses cn() for class merging. ActivityTimeline has Badge-styled action types. PatientContextSummary has skeleton loading. ProcedureSearch is wrapped in Card with Button.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-system-and-ui-overhaul/04-05-SUMMARY.md`
</output>
