---
phase: 04-real-time-system-and-ui-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/StructuredNote.tsx
  - src/components/DictationInput.tsx
  - src/components/SupplyChecklist.tsx
  - src/components/HandoffReport.tsx
autonomous: true
requirements:
  - UI-01
  - UI-06

must_haves:
  truths:
    - "StructuredNote uses shadcn Card with SoapSkeleton during processing"
    - "DictationInput uses shadcn Textarea + Button with skeleton integration and timeout retry"
    - "SupplyChecklist uses shadcn Table + Checkbox per D-11"
    - "HandoffReport uses shadcn Card with HandoffSkeleton during loading"
    - "All components use cn() for class merging"
    - "15-second timeout triggers TimeoutRetry component per D-16"
  artifacts:
    - path: "src/components/StructuredNote.tsx"
      provides: "shadcn Card SOAP note display"
      contains: "Card"
    - path: "src/components/DictationInput.tsx"
      provides: "shadcn Textarea + Button dictation input"
      contains: "Textarea"
    - path: "src/components/SupplyChecklist.tsx"
      provides: "shadcn Table + Checkbox supply checklist"
      contains: "TableHeader"
    - path: "src/components/HandoffReport.tsx"
      provides: "shadcn Card handoff report"
      contains: "Card"
  key_links:
    - from: "src/components/DictationInput.tsx"
      to: "src/components/skeletons/SoapSkeleton.tsx"
      via: "import during processing state"
      pattern: "SoapSkeleton"
    - from: "src/components/SupplyChecklist.tsx"
      to: "@/components/ui/checkbox"
      via: "import for per-item confirmation"
      pattern: "Checkbox"
---

<objective>
Migrate the four main content components (StructuredNote, DictationInput, SupplyChecklist, HandoffReport) to shadcn/ui with skeleton loader integration.

Purpose: These components display the core clinical content — notes, dictation, supplies, handoffs. Migrating them with skeleton loaders replaces static "Processing..." text with structured skeleton previews per D-14/D-15/D-16.
Output: Four migrated components with shadcn primitives, skeleton loaders, and timeout retry support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-CONTEXT.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
@.planning/phases/04-real-time-system-and-ui-overhaul/04-01-SUMMARY.md

<interfaces>
From src/lib/types.ts:
```typescript
export interface StructuredNote {
  subjective: string; history_of_present_illness?: string; comorbidities?: string;
  objective: string; assessment: string; interventions?: string; plan: string;
}
export interface Note {
  id: string; patient_id: string; raw_input: string; structured_note: StructuredNote;
  shift: string; nurse_name: string; flagged: boolean; flag_reason: string;
  created_at: string; review_status: ReviewStatus; reviewed_by: string | null; reviewed_at: string | null;
}
export interface SupplyItem { item: string; quantity: number; unit: string; notes: string; }
export interface SupplyRequest {
  id: string; patient_id: string; procedure: string; items: SupplyItem[];
  generated_at: string; confirmed_by: string | null; rationale: string | null;
  confirmed_items: Record<string, boolean>; note_id: string | null;
}
export interface WebhookResponse { /* note, supply_list, handoff_report */ }
```

From Plan 01 skeleton components:
```typescript
export function SoapSkeleton()    // from @/components/skeletons/SoapSkeleton
export function SupplySkeleton()  // from @/components/skeletons/SupplySkeleton
export function HandoffSkeleton() // from @/components/skeletons/HandoffSkeleton
export function TimeoutRetry({ onRetry }: { onRetry: () => void }) // from @/components/TimeoutRetry
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate StructuredNote and DictationInput to shadcn with skeleton integration</name>
  <files>src/components/StructuredNote.tsx, src/components/DictationInput.tsx</files>
  <read_first>
    - src/components/StructuredNote.tsx
    - src/components/DictationInput.tsx
    - src/components/ui/card.tsx
    - src/components/ui/button.tsx
    - src/components/ui/textarea.tsx
    - src/components/skeletons/SoapSkeleton.tsx
    - src/components/TimeoutRetry.tsx
    - src/lib/demo-scripts.ts
    - .planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
  </read_first>
  <action>
    **StructuredNote.tsx rewrite:**

    Replace custom container with shadcn Card. Import: Card, CardHeader, CardContent from `@/components/ui/card`, Badge from `@/components/ui/badge`, Button from `@/components/ui/button`, cn from `@/lib/utils`.

    Structure:
    1. Wrap each note in `<Card>` with conditional highlight ring (when `isHighlighted` prop is true, add `ring-2 ring-accent`)
    2. `<CardHeader>`: Note number + timestamp + nurse name on left, FlagBadge on right, flex justify-between
    3. `<CardContent>`: SOAP sections displayed with clear visual hierarchy
       - Each SOAP section (Subjective, Objective, Assessment, Plan): section label in `text-xs font-medium uppercase tracking-wide text-secondary mb-1` + content in `text-sm text-primary leading-relaxed`
       - Optional sections (HPI, Comorbidities, Interventions) only shown when present
       - Sections separated by `space-y-4`
    4. If note is flagged: show flag reason in a subtle warning/critical background strip below CardHeader — use `bg-flag-warning-bg` or `bg-flag-critical-bg` with appropriate text color
    5. If `hasSupplyList` prop: show a small link/button to switch to supplies tab
    6. NurseActionBar at bottom of card (keep existing integration)

    Keep ALL existing props and functionality. Replace template literals with cn(). Named export.

    **DictationInput.tsx rewrite:**

    Replace native textarea and custom buttons with shadcn Textarea + Button. Import: Textarea from `@/components/ui/textarea`, Button from `@/components/ui/button`, Card/CardHeader/CardContent from `@/components/ui/card`, cn from `@/lib/utils`, SoapSkeleton from `@/components/skeletons/SoapSkeleton`, TimeoutRetry from `@/components/TimeoutRetry`.

    Key changes:
    1. Wrap entire input area in `<Card>`
    2. `<CardHeader>`: "Clinical Dictation" title
    3. Replace `<textarea>` with `<Textarea>` (shadcn). Keep existing value binding, onChange, rows, placeholder
    4. Replace custom buttons with `<Button>`:
       - "Begin Dictation" (typewriter mode): `variant="default"` (accent bg)
       - "Submit Note" (free-form mode): `variant="default"`
       - Script selector stays as a shadcn Select if it was already migrated, or keep as-is and use cn()
    5. **Skeleton integration per D-14, D-16:**
       - When state is `'processing'`: show `<SoapSkeleton />` instead of "Processing..." text
       - Add a `useEffect` timer: after 15 seconds of processing state, set `showTimeout` to true
       - When `showTimeout` is true AND still processing: overlay `<TimeoutRetry onRetry={handleRetry} />` below the skeleton
       - `handleRetry` function: resubmit the same webhook request (keep the last raw_input in a ref or state)
       - Reset `showTimeout` to false when processing completes or errors
    6. Keep ALL existing functionality: typewriter animation, demo script selector, free-form mode, webhook submission, error handling with toast

    Replace all template literals with cn(). Named export.
  </action>
  <verify>
    <automated>cd /Users/gozzynwogbo/ai_domain/n8n/nursingworkflow/nurse_context_engine/src && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StructuredNote.tsx contains import from "@/components/ui/card"
    - src/components/StructuredNote.tsx contains "Card" and "CardHeader" and "CardContent"
    - src/components/StructuredNote.tsx contains "cn("
    - src/components/StructuredNote.tsx contains "Subjective" and "Objective" and "Assessment" and "Plan"
    - src/components/DictationInput.tsx contains import from "@/components/ui/textarea"
    - src/components/DictationInput.tsx contains import from "@/components/ui/button"
    - src/components/DictationInput.tsx contains "SoapSkeleton"
    - src/components/DictationInput.tsx contains "TimeoutRetry"
    - src/components/DictationInput.tsx contains "15000" or "15 * 1000" or "15_000" (15-second timeout)
    - src/components/DictationInput.tsx contains "cn("
    - npm run build succeeds
  </acceptance_criteria>
  <done>StructuredNote displays SOAP sections in shadcn Card with visual hierarchy, DictationInput uses shadcn Textarea/Button with SoapSkeleton during processing and TimeoutRetry after 15 seconds</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SupplyChecklist and HandoffReport to shadcn</name>
  <files>src/components/SupplyChecklist.tsx, src/components/HandoffReport.tsx</files>
  <read_first>
    - src/components/SupplyChecklist.tsx
    - src/components/HandoffReport.tsx
    - src/components/ui/table.tsx
    - src/components/ui/checkbox.tsx
    - src/components/ui/card.tsx
    - src/components/ui/button.tsx
    - src/components/skeletons/SupplySkeleton.tsx
    - src/components/skeletons/HandoffSkeleton.tsx
    - src/lib/types.ts
    - .planning/phases/04-real-time-system-and-ui-overhaul/04-UI-SPEC.md
  </read_first>
  <action>
    **SupplyChecklist.tsx rewrite** per D-11:

    Replace custom table and checkboxes with shadcn Table + Checkbox. Import: Table, TableHeader, TableBody, TableRow, TableHead, TableCell from `@/components/ui/table`, Checkbox from `@/components/ui/checkbox`, Button from `@/components/ui/button`, Card/CardHeader/CardContent from `@/components/ui/card`, cn from `@/lib/utils`.

    Structure:
    1. Wrap in `<Card>`
    2. `<CardHeader>`: Procedure name + "Procedure Prep Recommendations" label + generated time + rationale line (if rationale prop exists, show "AI suggested based on..." per AICTX-03)
    3. `<CardContent>`:
       - `<Table>` with `<TableHeader>` row: Item | Qty | Unit | Notes | Confirmed
       - `<TableBody>`: each SupplyItem as `<TableRow>` with `<TableCell>` for each column
       - Confirmed column: `<Checkbox>` from shadcn (Radix primitive with ARIA checked state, keyboard Space to toggle)
       - Checkbox `checked` state from `confirmedItems[item.item]` (or index-based key)
       - Checkbox `onCheckedChange` calls the existing toggle function that writes to Supabase
    4. Below table: "Mark All Ready" Button (variant="default") + "Confirm Prepared" per-row is replaced by the Checkbox in the table
    5. "Mark All Ready" button disabled when all items already confirmed

    Keep ALL existing functionality:
    - Optimistic UI updates with revert on Supabase error
    - Confirmed items persistence via `confirmed_items` JSONB
    - Audit entry on confirm-supply
    - Toast feedback

    **HandoffReport.tsx rewrite:**

    Replace custom layout with shadcn Card components. Import: Card, CardHeader, CardContent from `@/components/ui/card`, Badge from `@/components/ui/badge`, cn from `@/lib/utils`.

    Structure per D-15 and UI-SPEC:
    1. Container: `space-y-4` for vertical card stack
    2. **Summary card**: `<Card>` with `<CardHeader>` "Summary" + generated timestamp + incoming shift label, `<CardContent>` with summary paragraph in text-sm leading-relaxed
    3. **Priority Flags card**: `<Card>` with header "Priority Flags", content listing each flag with Badge variant matching flag type (warning for 'warning', critical for 'critical') + detail text
    4. **Stable Items card**: `<Card>` with header "Stable Items", content as bulleted list of stable items
    5. **Recommended Actions card**: `<Card>` with header "Recommended First Actions", content as numbered/bulleted list of recommended actions

    Keep all existing props. Replace template literals with cn(). Named export.

    If no flags/stableItems/recommendedActions, still show the card with empty state text per UI-SPEC: e.g., "No priority flags" or "No stable items recorded".
  </action>
  <verify>
    <automated>cd /Users/gozzynwogbo/ai_domain/n8n/nursingworkflow/nurse_context_engine/src && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SupplyChecklist.tsx contains import from "@/components/ui/table"
    - src/components/SupplyChecklist.tsx contains "TableHeader" and "TableBody" and "TableRow" and "TableCell"
    - src/components/SupplyChecklist.tsx contains import from "@/components/ui/checkbox"
    - src/components/SupplyChecklist.tsx contains "Checkbox"
    - src/components/SupplyChecklist.tsx contains "cn("
    - src/components/HandoffReport.tsx contains import from "@/components/ui/card"
    - src/components/HandoffReport.tsx contains "Card" and "CardHeader" and "CardContent"
    - src/components/HandoffReport.tsx contains "Summary" and "Priority Flags" and "Stable Items" and "Recommended"
    - src/components/HandoffReport.tsx contains "cn("
    - npm run build succeeds
  </acceptance_criteria>
  <done>SupplyChecklist uses shadcn Table + Checkbox with proper ARIA states and keyboard toggle, HandoffReport renders four-card layout with summary/flags/stable/actions sections using shadcn Card</done>
</task>

</tasks>

<verification>
- `cd src && npm run build` passes
- `cd src && npm run lint` passes
- SupplyChecklist renders shadcn Table with Checkbox per row
- HandoffReport renders 4 card sections
- DictationInput shows SoapSkeleton during processing
- TimeoutRetry appears after 15 seconds of processing
</verification>

<success_criteria>
All four content components use shadcn primitives. Skeleton loaders replace static "Processing..." text. SupplyChecklist has accessible checkboxes with keyboard support. HandoffReport has professional four-card layout. 15-second timeout triggers retry option.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-system-and-ui-overhaul/04-04-SUMMARY.md`
</output>
