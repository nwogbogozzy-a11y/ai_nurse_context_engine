---
phase: 03-ai-context-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/004_phase3_context_memory.sql
  - src/lib/types.ts
  - n8n-workflow.json
autonomous: true
requirements: [AICTX-01, AICTX-03, AICTX-04]

must_haves:
  truths:
    - "When a nurse submits a new note, the Claude prompt includes the patient's last 5 notes (Assessment + Plan sections) with age annotations"
    - "After a note is saved, a patient context summary is generated by Claude and stored in patient_summaries table"
    - "Supply recommendations include a rationale string explaining why the supplies were recommended"
  artifacts:
    - path: "supabase/migrations/004_phase3_context_memory.sql"
      provides: "patient_summaries table with UNIQUE on patient_id, rationale column on supply_requests"
      contains: "CREATE TABLE public.patient_summaries"
    - path: "src/lib/types.ts"
      provides: "PatientSummary interface, rationale on WebhookResponse supply_list, rationale on SupplyRequest"
      exports: ["PatientSummary"]
    - path: "n8n-workflow.json"
      provides: "N3b prior notes fetch, modified N4/N8a/N8b prompts with prior_notes_context, N6b/N6c summary generation"
      contains: "prior_notes_context"
  key_links:
    - from: "n8n N3b node"
      to: "Supabase notes table"
      via: "REST API GET with limit=5 order by created_at desc"
      pattern: "notes.*patient_id.*limit=5"
    - from: "n8n N4 Claude prompt"
      to: "N3b output"
      via: "prior_notes_context injected into prompt"
      pattern: "prior_notes_context"
    - from: "n8n N6b Claude summary"
      to: "N6c Save Summary"
      via: "upsert to patient_summaries"
      pattern: "patient_summaries"
---

<objective>
Add AI context memory to the n8n backend pipeline: prior notes retrieval, prompt enrichment, patient summary generation, and supply rationale support.

Purpose: Enable the "system remembers" narrative by feeding patient history into every Claude API call and generating rolling patient summaries.
Output: Updated n8n workflow JSON, new Supabase migration, extended TypeScript types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-context-memory/03-CONTEXT.md
@.planning/phases/03-ai-context-memory/03-RESEARCH.md

<interfaces>
<!-- Current types from src/lib/types.ts -->
```typescript
export interface SupplyItem {
  item: string
  quantity: number
  unit: string
  notes: string
}

export interface SupplyRequest {
  id: string
  patient_id: string
  procedure: string
  items: SupplyItem[]
  generated_at: string
  confirmed_by: string | null
}

export interface WebhookResponse {
  success: boolean
  note: {
    structured_note: StructuredNote
    flagged: boolean
    flag_reason: string
    procedures: string[]
    patient_id: string
    nurse_name: string
    shift: string
  }
  supply_list?: {
    procedure: string
    items: SupplyItem[]
  }
  handoff_report?: {
    summary: string
    priority_flags: PriorityFlag[]
    stable_items: string[]
    recommended_first_actions: string[]
  }
}
```

<!-- Existing migrations -->
- `supabase/migrations/001_enable_rls.sql` -- RLS on all 4 tables
- `supabase/migrations/002_phase1_persistence.sql` -- review_status, reviewed_by, reviewed_at on notes; confirmed_items on supply_requests; stable_items/recommended_first_actions on handoff_reports
- `supabase/migrations/003_audit_log.sql` -- audit_log table and trigger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database migration and extend TypeScript types</name>
  <files>supabase/migrations/004_phase3_context_memory.sql, src/lib/types.ts</files>
  <read_first>
    - supabase/migrations/003_audit_log.sql
    - supabase/migrations/002_phase1_persistence.sql
    - src/lib/types.ts
  </read_first>
  <action>
    Create `supabase/migrations/004_phase3_context_memory.sql` with:

    ```sql
    -- Patient summaries table for AI-generated rolling context
    CREATE TABLE public.patient_summaries (
      id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
      patient_id UUID NOT NULL REFERENCES public.patients(id) UNIQUE,
      summary TEXT NOT NULL,
      note_count INTEGER NOT NULL DEFAULT 0,
      generated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    -- RLS policies for patient_summaries
    ALTER TABLE public.patient_summaries ENABLE ROW LEVEL SECURITY;
    CREATE POLICY "Allow anon select on patient_summaries" ON public.patient_summaries FOR SELECT TO anon USING (true);
    CREATE POLICY "Allow anon insert on patient_summaries" ON public.patient_summaries FOR INSERT TO anon WITH CHECK (true);
    CREATE POLICY "Allow anon update on patient_summaries" ON public.patient_summaries FOR UPDATE TO anon USING (true) WITH CHECK (true);

    -- Add rationale column to supply_requests
    ALTER TABLE public.supply_requests ADD COLUMN IF NOT EXISTS rationale TEXT;
    ```

    Run this migration against Supabase using the Supabase CLI or SQL editor.

    Then update `src/lib/types.ts`:

    1. Add PatientSummary interface after AuditLogEntry:
    ```typescript
    export interface PatientSummary {
      id: string
      patient_id: string
      summary: string
      note_count: number
      generated_at: string
      created_at: string
    }
    ```

    2. Add `rationale` to SupplyRequest interface:
    ```typescript
    export interface SupplyRequest {
      id: string
      patient_id: string
      procedure: string
      items: SupplyItem[]
      generated_at: string
      confirmed_by: string | null
      rationale: string | null  // AI explanation of why these supplies
    }
    ```

    3. Add `rationale` to WebhookResponse.supply_list:
    ```typescript
    supply_list?: {
      procedure: string
      rationale?: string  // AI explanation of recommendations per D-08
      items: SupplyItem[]
    }
    ```

    Per D-06, patient_summaries is a dedicated table (not a column on patients) to keep patients table clean and enable future summary history.
    Per D-08, rationale is per-checklist (single string), not per-item.
  </action>
  <verify>
    <automated>cd src && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/004_phase3_context_memory.sql contains `CREATE TABLE public.patient_summaries`
    - supabase/migrations/004_phase3_context_memory.sql contains `patient_id UUID NOT NULL REFERENCES public.patients(id) UNIQUE`
    - supabase/migrations/004_phase3_context_memory.sql contains `ALTER TABLE public.supply_requests ADD COLUMN`
    - supabase/migrations/004_phase3_context_memory.sql contains `rationale TEXT`
    - src/lib/types.ts contains `export interface PatientSummary`
    - src/lib/types.ts contains `rationale: string | null` inside SupplyRequest
    - src/lib/types.ts contains `rationale?: string` inside WebhookResponse supply_list block
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Migration file creates patient_summaries table with UNIQUE patient_id constraint and adds rationale column to supply_requests. TypeScript types include PatientSummary interface and rationale fields on SupplyRequest and WebhookResponse.</done>
</task>

<task type="auto">
  <name>Task 2: Add N3b prior notes fetch and modify Claude prompts in main n8n workflow</name>
  <files>n8n-workflow.json</files>
  <read_first>
    - n8n-workflow.json
    - .planning/phases/03-ai-context-memory/03-RESEARCH.md
  </read_first>
  <action>
    Modify `n8n-workflow.json` to add prior notes context to Claude prompts. Per D-03, all prior notes fetching happens server-side in n8n.

    **IMPORTANT:** The n8n workflow JSON uses string-based node name references. After every modification, verify all `$('...')` expressions reference correct node names. Use the n8n MCP tools (if available) or carefully edit the JSON directly.

    **Step 1: Add N3b Fetch Prior Notes node**
    Insert an HTTP Request node after the existing N3 Fetch Patient Context node and before N4. This node queries Supabase REST API:
    - Method: GET
    - URL: `={{ $env.SUPABASE_URL }}/rest/v1/notes?patient_id=eq.{{ $('N2 Parse Input').item.json.patient_id }}&select=structured_note,created_at&order=created_at.desc&limit=5`
    - Headers: `apikey: {{ $env.SUPABASE_ANON_KEY }}`, `Authorization: Bearer {{ $env.SUPABASE_ANON_KEY }}`
    - Response: Array of {structured_note, created_at} objects

    **Step 2: Add N3c Parse Prior Notes (Set node)**
    A Set node after N3b that extracts Assessment + Plan from each note with age annotation per D-02:
    ```javascript
    // Expression for prior_notes_context field:
    (() => {
      const notes = $('N3b Fetch Prior Notes').all()
      const now = new Date()
      if (!notes || notes.length === 0) return 'No prior notes available.'

      return 'Prior clinical context (last ' + notes.length + ' notes, most recent first):\n\n' +
        notes.map((n, i) => {
          let parsed
          try {
            parsed = typeof n.json.structured_note === 'string'
              ? JSON.parse(n.json.structured_note)
              : n.json.structured_note
          } catch (e) { return `[${i + 1}] Unable to parse note` }
          const created = new Date(n.json.created_at)
          const hoursAgo = Math.round((now - created) / 3600000)
          const ageLabel = hoursAgo < 24
            ? hoursAgo + ' hours ago'
            : Math.round(hoursAgo / 24) + ' days ago'
          return '[' + (i + 1) + '] ' + ageLabel + ' (' + created.toLocaleTimeString('en-GB', {hour:'2-digit', minute:'2-digit'}) + '):\n  Assessment: ' + (parsed.assessment || 'N/A') + '\n  Plan: ' + (parsed.plan || 'N/A')
        }).join('\n\n')
    })()
    ```
    Also pass through all fields from N3 Fetch Patient Context and N2 Parse Input.

    **Step 3: Modify N4 Claude Structure Note prompt**
    Add prior notes context to the Claude prompt. Insert after the existing patient context block:
    ```
    Prior clinical context (last 5 notes, most recent first):
    {{ $('N3c Parse Prior Notes').item.json.prior_notes_context }}

    Use the prior clinical context to inform your assessment and plan. Reference relevant history where it affects the current note (e.g., "consistent with prior assessments" or "new finding compared to previous check"). Do not repeat prior notes verbatim.
    ```

    **Step 4: Modify N8a Claude Supply List prompt**
    Add prior notes context and rationale to the supply prompt. Add before the JSON return schema:
    ```
    Prior clinical context:
    {{ $('N3c Parse Prior Notes').item.json.prior_notes_context }}

    Include a rationale explaining why these specific supplies are recommended given the procedure and patient context.
    ```
    Update the JSON return schema to include rationale:
    ```json
    {
      "procedure": "",
      "rationale": "",
      "items": [
        { "item": "", "quantity": 0, "unit": "", "notes": "" }
      ]
    }
    ```

    **Step 5: Modify N8b Claude Handoff Report prompt**
    Add prior notes context per D-01 (handoff uses same 5-note window):
    ```
    Prior clinical context (last 5 notes):
    {{ $('N3c Parse Prior Notes').item.json.prior_notes_context }}
    ```

    **Step 6: Update N9a Save Supply Request**
    Add rationale field to the Supabase INSERT body so the rationale gets persisted:
    Include `"rationale": "{{ $json.rationale }}"` in the request body alongside existing procedure and items fields.

    **Step 7: Update node connections**
    - Disconnect N3 -> N4 connection
    - Connect N3 -> N3b -> N3c -> N4
    - All downstream nodes that referenced N3 output now reference N3c (which passes through N3 data plus prior_notes_context)
    - Verify N5 Parse Claude Response still correctly references N4 output
    - Position N3b and N3c between N3 and N4 in the workflow canvas (adjust x/y positions)

    Per RESEARCH.md Pitfall 2: structured_note is stored as TEXT (JSON-stringified) in Supabase. The N3c Set node MUST JSON.parse() the structured_note before extracting .assessment and .plan.
  </action>
  <verify>
    <automated>cat n8n-workflow.json | python3 -c "import sys,json; d=json.load(sys.stdin); nodes=[n['name'] for n in d['nodes']]; print('Nodes:', nodes); assert any('N3b' in n or 'Prior' in n for n in nodes), 'N3b node missing'; print('OK: N3b node found')"</automated>
  </verify>
  <acceptance_criteria>
    - n8n-workflow.json contains a node with name containing "N3b" or "Prior Notes"
    - n8n-workflow.json contains a node with name containing "N3c" or "Parse Prior"
    - n8n-workflow.json contains the string "prior_notes_context" in at least one node
    - n8n-workflow.json contains the string "rationale" in the N8a supply prompt area
    - n8n-workflow.json N4 Claude prompt contains "Prior clinical context"
    - n8n-workflow.json N8b handoff prompt contains "Prior clinical context"
    - n8n-workflow.json is valid JSON (python3 -c "import json; json.load(open('n8n-workflow.json'))" succeeds)
  </acceptance_criteria>
  <done>Main n8n workflow includes N3b prior notes fetch, N3c parse with age annotations, all three Claude prompts (N4, N8a, N8b) inject prior_notes_context, supply prompt returns rationale field, N9a saves rationale to Supabase.</done>
</task>

<task type="auto">
  <name>Task 3: Add patient summary generation nodes (N6b, N6c) to n8n workflow</name>
  <files>n8n-workflow.json</files>
  <read_first>
    - n8n-workflow.json
  </read_first>
  <action>
    Add patient summary generation as a branch after N6 Save Note, per D-05 (summary generated on note creation only).

    **N6b Generate Patient Summary** -- Claude API HTTP Request node:
    - Method: POST
    - URL: https://api.anthropic.com/v1/messages
    - Headers: x-api-key: {{ $env.ANTHROPIC_API_KEY }}, anthropic-version: 2023-06-01, content-type: application/json
    - Body:
    ```json
    {
      "model": "claude-sonnet-4-20250514",
      "max_tokens": 512,
      "messages": [{
        "role": "user",
        "content": "You are a clinical summarization assistant. Given a patient's record and their recent notes, produce a concise patient context summary that an incoming nurse could read in 30 seconds.\n\nPrioritize: current status, active concerns, treatment trajectory, what needs watching.\nWrite as if briefing a colleague verbally -- short, prioritized, clinical.\n\nPatient record: {{ $('N3c Parse Prior Notes').item.json.patient_context }}\nRecent notes (last 5, A+P only): {{ $('N3c Parse Prior Notes').item.json.prior_notes_context }}\nLatest note: {{ $('N5 Parse Claude Response').item.json.structured_note }}\n\nReturn a plain text summary, 3-5 sentences. No JSON wrapping. No headers."
      }]
    }
    ```
    - Max tokens: 512

    **N6c Save Patient Summary** -- HTTP Request node (Supabase upsert):
    - Method: POST
    - URL: {{ $env.SUPABASE_URL }}/rest/v1/patient_summaries
    - Headers:
      - apikey: {{ $env.SUPABASE_ANON_KEY }}
      - Authorization: Bearer {{ $env.SUPABASE_ANON_KEY }}
      - Content-Type: application/json
      - Prefer: resolution=merge-duplicates
    - Body:
    ```json
    {
      "patient_id": "{{ $('N2 Parse Input').item.json.patient_id }}",
      "summary": "{{ $('N6b Generate Patient Summary').item.json.content[0].text }}",
      "note_count": {{ $('N3b Fetch Prior Notes').all().length + 1 }},
      "generated_at": "{{ $now.toISO() }}"
    }
    ```

    **Connections:**
    - N6 Save Note connects to BOTH N7 Router (existing) AND N6b Generate Patient Summary (new parallel branch)
    - N6b connects to N6c
    - N6c is a terminal node (no downstream connections)
    - This means summary generation runs in parallel with the router/response path per RESEARCH.md architecture pattern

    **Positioning:** Place N6b below N7 Router on the canvas (y offset +200), N6c to the right of N6b.

    Per RESEARCH.md Pitfall 3: The summary generation branches off N6 in parallel with N7 Router. Since n8n supports multiple output connections from a single node, the webhook response is NOT blocked by summary generation.

    Per RESEARCH.md Pitfall 4: The Supabase REST upsert uses `Prefer: resolution=merge-duplicates` header, which requires the UNIQUE constraint on patient_id that was created in Task 1's migration.
  </action>
  <verify>
    <automated>cat n8n-workflow.json | python3 -c "import sys,json; d=json.load(sys.stdin); nodes=[n['name'] for n in d['nodes']]; print('Nodes:', nodes); assert any('N6b' in n or 'Summary' in n for n in nodes), 'N6b missing'; assert any('N6c' in n or 'Save Summary' in n or 'Save Patient' in n for n in nodes), 'N6c missing'; print('OK: Summary nodes found')"</automated>
  </verify>
  <acceptance_criteria>
    - n8n-workflow.json contains a node with name containing "N6b" or "Patient Summary" or "Generate"
    - n8n-workflow.json contains a node with name containing "N6c" or "Save Summary" or "Save Patient"
    - n8n-workflow.json N6b node body contains "clinical summarization assistant"
    - n8n-workflow.json N6c node URL contains "patient_summaries"
    - n8n-workflow.json N6c node headers contain "resolution=merge-duplicates"
    - The N6 Save Note node has connections to both N7 Router and N6b
    - n8n-workflow.json is valid JSON
  </acceptance_criteria>
  <done>N6b generates a patient context summary via Claude after every note save. N6c upserts the summary to patient_summaries table. Summary generation runs in parallel with the webhook response path, not blocking it.</done>
</task>

</tasks>

<verification>
1. `cd src && npx tsc --noEmit` -- TypeScript compiles with no errors
2. `python3 -c "import json; json.load(open('n8n-workflow.json'))"` -- workflow JSON is valid
3. n8n-workflow.json contains nodes: N3b, N3c, N6b, N6c (or equivalent names)
4. All three Claude prompts (N4, N8a, N8b) contain "prior_notes_context" or "Prior clinical context"
5. Migration file creates patient_summaries table and adds rationale column
</verification>

<success_criteria>
- Migration SQL ready to run against Supabase (creates patient_summaries table with UNIQUE patient_id, adds rationale to supply_requests)
- TypeScript types include PatientSummary interface and rationale fields
- n8n workflow has N3b/N3c nodes that fetch and parse last 5 notes with age annotations
- All three Claude prompts enriched with prior clinical context
- Supply prompt returns rationale field; N9a persists rationale to Supabase
- Patient summary generated via N6b/N6c after every note creation, stored via upsert
- Summary generation runs parallel to webhook response (not blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-context-memory/03-01-SUMMARY.md`
</output>
